export type EnvType = {
  nodeEnv: string;
  jwtSecret: string;
};

export type DatabaseEnvType = {
  host: string;
  port: number;
  username: string;
  password: string;
  name: string;
  url: string;
};
